#include "include/talkTic.h"
/*
Concept concept
{
public:
	method()
	{

	}
	//virtual void call();
};
*/
/*auto function()
{

	std::cin >> (auto result);

}*/

/*
int main(){

int y = 100;
auto lambda = [=](int x) { return x + y; }

std::cout << lambda(2);
}
*/

// define averager as routine	 define x And define y And define z
// 	first define ave is (x + y +z )/3
// 	then passback ave
// finish

define SayHi subroutine
	first write "Simple Factor Finder App Using My talkTic C++ Customizer:"
finish

app
	first SayHi
	_
	then always
		first define n is 0
		then write enter
		then write "Please enter n: "
		then get n
		then check n is -1
			ifyes fuckoff

		finish
		then dothis from i is 1 until i isgreaterthan n forstep i++
			first check n mod i equals 0
				first write i
				then write enter
			finish
		finish
		then hangon
	finish
finish

//operator overloading
